# ==============================
# Topic: COMBINED LOOPS + FUNCTIONS + CLASSES
# Level: Advanced Beginner / Intermediate
# Instructions:
# - Use loops inside methods
# - Functions can be standalone or class methods
# - Solve using basic Python constructs only
# ==============================


# Q1.
# Create a class Student with attributes:
# - name
# - marks (list of integers)
# Add a method grade() that returns:
# - 'A' if average >= 90
# - 'B' if average >= 75
# - 'C' if average >= 50
# - 'F' otherwise


# Q2.
# Create a class ShoppingCart with attribute:
# - items (list of tuples, each tuple = (item_name, price))
# Add methods:
# - add_item(item_name, price)
# - total() returns the sum of all item prices
# - most_expensive() returns the name of the most expensive item


# Q3.
# Write a function that takes a list of Student objects
# and prints the name of the student with the highest average marks


# Q4.
# Create a class Library with attribute:
# - books (dictionary with book title as key, copies as value)
# Add methods:
# - borrow(book_title) reduces copy by 1 if available
# - return_book(book_title) increases copy by 1
# - available_books() prints all books with copies > 0


# Q5.
# Create a class NumberStats with attribute:
# - numbers (list of integers)
# Add methods:
# - sum() returns sum of numbers
# - max_min() returns a tuple (max, min)
# - even_numbers() returns a list of all even numbers
